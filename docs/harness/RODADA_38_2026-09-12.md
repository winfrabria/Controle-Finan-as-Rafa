# Rodada38 — confirmação por achado e comparação entre múltiplas fontes

Somente a worktree `F:\winfra-worktrees\harness-universal-20260907`, ramo `codex/harness-universal-20260907`. Sem integração, commit, push, deploy ou promoção de shadow para enforce. Política e prompt `2026-09-12.1`; schema e regras `2026-09-08.22`.

## Correções genéricas

1. Confirmação individual é separada de cobertura integral. Sob cobertura parcial, somente conflitos DOCUMENT_CONTENT com a mesma hipótese, valores, páginas, fontes, escopo e citações próprias podem sustentar suspeita. Não permite PASS integral, autorização, identidade ou hipóteses sem confirmação. Respostas estruturadas em quarentena podem fornecer apenas esses conflitos individualmente válidos; o AiRun permanece FAILED e sua cobertura sempre limitada.
2. Hipóteses com três ou mais fontes não são reduzidas às duas primeiras posições. O par indicado deve conter os valores realmente conflitantes, rastreáveis às fontes originais; todas as fontes continuam exigindo citação. Comparar duas fontes concordantes, repetir o mesmo índice, alterar valor/escopo ou omitir evidência continua inválido. A regra de pares fixos sourcePair permanece separada e não foi relaxada.
3. Descoberta salva ganha hash persistido. Replay valida contexto integral, versões de origem, hash de arquivo e hipóteses; alteração não compra verificação. Revalidação offline permite somente resposta final SUCCEEDED do mesmo documento/contexto, schema compatível e fingerprint original. Não chama provedor, não cria outra execução de verificação, não reescreve histórico/custo e identifica no novo diagnóstico o run de origem. Não é uma nova leitura pelo prompt atualizado.
4. No card resumido, “Onde encontramos” prioriza fontes com valores distintos em vez de esconder a divergente atrás de duas fontes concordantes. O detalhe conserva todas as fontes. Formatos equivalentes de moeda/data não contam como valores diferentes. `sourceDate` passa a “Data do documento”. Identidade visual preservada.

## Documento real e custo

Uma única chamada paga nesta rodada, com Gemini3.8/medium/32768: **56,764s e US$0,06695925**, run `be5dd89a-397b-4850-8ca6-4dabd1774ba7`. Leitura e descoberta foram reaproveitadas com custo novo zero. Relatório privado `tmp/reaudit-snapshot-dc02425e-ff76-42af-9c7e-8a2af3f2dfba.json` preserva entrada sem URL assinada e resposta final estruturada, nunca raciocínio interno ou stream parcial.

A resposta confirmou três hipóteses. A primeira aplicação publicou duas: o check do item19 comparava recibo e cartão (índices1/2, ambos na página20), mas o validador exigia as páginas das duas primeiras observações (1/20). A evidência estava correta; a suposição posicional do código estava errada. A correção foi validada contra fontes sintéticas em outras posições/páginas e valores. Não houve outra compra de IA.

Revalidação offline: `tmp/reaudit-snapshot-eda77a2d-4bf1-4a7a-8161-5b8013de4e6c.json`. **Roger versão102, COMPLETED/SUSPICIOUS/LIMITED**, com três achados AI_VERIFICATION: venda44,50 versus ficha/pagamento40,00; ficha/recibo18,00 versus débito28,00; ficha19/05/2026 versus pedido18/05/2026. Sem pergunta automática. A comparação visual foi refeita nas páginas1,9,13,20 do PDF recuperado do armazenamento local; SHA-256 idêntico ao original registrado. O caminho anterior em Downloads não estava mais disponível. Isso não prova conferência visual de todas as23 páginas nesta rodada.

O registro de verificação original continua com seu custo e cobertura histórica; o novo audit registra `revalidatedStoredResponse=true`. A extração continua limitada, com lacunas já documentadas; fornecedor do reembolso ainda veio do cabeçalho HWN. Os três conflitos não comprovam fraude, qual valor é correto, nem autorização/rejeição da despesa.

## Validação

615 testes Harness passaram com banco isolado, zero falhas/pulados (`tmp/test-v38-pair-final.log`), incluindo confirmação parcial, quarentena, revalidação sem chamadas, contexto alterado e pares de fontes. Detalhe37/37, lista/envio69/69 após as mudanças de apresentação, PWA36/36. Logs `tmp/note-detail-v38.log`, `tmp/upload-v38-final.log`, `tmp/pwa-v38.log`. Lint/TypeScript/build finais em `tmp/check-v38-ui-final.log` e `tmp/build-v38-final.log`. Falhas intermediárias de versão esperada e tipagem do novo resumo foram corrigidas, não ignoradas.

Não houve novo teste integral de upload/OCR nesta rodada: o ensaio do Roger reaproveitou extração e descoberta. O tempo56,764s é de uma verificação, não tempo de envio até conclusão nem benchmark geral. PWA físico, instalação/offline no aparelho e operação completa do histórico de envios no navegador continuam sem homologação. Nenhum resultado sintético foi apresentado como qualidade documental ou prontidão para produção.

## Conferência da build final

Lista autenticada: três achados do Roger, rótulo “Data do documento” e resumo do item19 com ficha18,00/pagamento28,00, mantendo a indicação de mais uma fonte no detalhe. Captura `tmp/ui-v38/02-desktop-evidence-final.png`: o painel tem rolagem própria e a imagem não mostra seu conteúdo inteiro. Essa limitação visual permanece; não é um aceite de UI impecável.

Detalhe mobile em388x840 CSS: navegação Próximo levou ao segundo achado e focou seu título; largura de rolagem365px, sem transbordamento horizontal. “Onde encontramos” mostrou ficha/página1, recibo/página20 e débito/página20, com links de página separados. Escape fechou o diálogo e devolveu foco ao acionador. Captura mobile falhou, sem arquivo final; o viewport foi restaurado. Ainda aparece o valor técnico SHEET/PAYMENT em “Fontes citadas”; revisão de apresentação não está encerrada.

Servidor da build final em127.0.0.1:3117. Estado local:38 jobs SUCCEEDED,7 CANCELLED históricos, nenhum job pendente/executando e nenhum AiRun RUNNING (`tmp/status-v38.json`). A rota pública de envio respondeu HTTP200. Nenhuma chamada paga ativa. Testes removeram somente seus próprios fixtures sintéticos; PDFs reais e histórico foram preservados.
