# Rodada 41 — releitura sem valores prévios e versão de teste atualizada

Somente `F:\winfra-worktrees\harness-universal-20260907`, ramo `codex/harness-universal-20260907`. Política `2026-09-12.4`, prompt `2026-09-12.3`, schema/regras `2026-09-08.22`. Sem integração, commit, push, deploy ou promoção para enforce. A identidade visual e o fluxo em segundo plano da rodada 40 foram preservados.

## O que mudou

O transporte do verificador passou a enviar um índice de páginas e fontes, sem repetir valores, datas, nomes, texto OCR e descrições da extração. Os alertas de regras locais conservam identificadores para roteamento, mas não fornecem valores e citações para copiar. A classificação anterior não é enviada. O arquivo tem nome neutro, preservando integralmente seus bytes. O índice não é prova de cobertura ou de vínculo correto; o verificador precisa conferir o original. A extração armazenada não é modificada.

**Limite importante:** hipóteses `AI_DISCOVERY` continuam completas, nas posições originais, porque a confirmação exige correspondência exata de código, escopo e alegações. Portanto, o fluxo geral não é uma verificação totalmente cega. Não houve relaxamento de validação, confirmação, cobertura ou evidência monetária. Regressões também cobrem extrações legadas sem observações/inventário.

## Ensaios reais, sem alterar diagnósticos

O script `scripts/probe-isolated-blind-page.ts` verifica nota LOCAL-104, SHA do PDF original, página e imagem. Renderiza a página inteira em PNG para leitura, sem editar ou reexportar o PDF. O pedido contém somente a imagem e o prompt genérico, não valores ou achados anteriores. Registra intenção antes da chamada e impede repetição acidental do mesmo ensaio; uma tentativa, sem fallback automático. Não escreve no banco.

A página 20 do Roger foi examinada visualmente e lida por dois modelos. Ambos retornaram pagamento **28,00**, contra **18,00** no comprovante manuscrito; a extração do upload anterior havia lido 26,00.

| Ensaio da página inteira | Duração | Custo informado | Resultado e limite |
| --- | --- | --- | --- |
| Gemini 3.7 low | 7,719s | US$0,0088665 | Valores 28,00/18,00 e descrição Lanche |
| GPT 5.6 Sol high | 39,901s | US$0,16608625 | Valores 28,00/18,00, mas descrição incorreta Gancho |

Esse resultado confirma apenas a leitura desse campo nessa página. Não comprova cobertura do PDF inteiro, precisão geral ou superioridade universal de modelo. O formato de entrada, a atenção a uma só página e a ausência de contexto prévio mudaram juntos; o ensaio não isola causalmente a influência da extração anterior.

Foi executado um ensaio do verificador com o **PDF original completo, 23 páginas e 24 checks**, Gemini 3.8 medium, novo índice, sem descoberta prévia. Terminou por timeout após **180,567s**, sem conteúdo recebido, custo desconhecido. A nota permaneceu inalterada. Uma consulta somente de metadados ao provedor retornou HTTP404; isso não comprova custo zero ou cancelamento remoto. Não houve repetição. Relatório privado: `tmp/verifier-model-97753b45-0372-4dd5-a4e1-886de005a323.json`; consulta: `tmp/verifier-generation-v41.json`.

Custo conhecido da rodada: **US$0,17495275**, mais esse custo desconhecido. Os relatórios privados dos dois ensaios de página estão em `tmp/blind-page-33de7877a1de3997fe277a63b57f0525669a56ea6599a42aa4d3c83d5adb8818.json` e `tmp/blind-page-577c37a788be389ee4774cbc3643672023a16b2f1b4b683bcb83ff8360bb059a.json`.

## Verificações e entrega para inspeção

638/638 testes Harness passaram com `HARNESS_DATABASE_TESTS=1`, sem falhas ou pulados (`tmp/test-v41-db-final.log`). Uma execução anterior sem a variável pulou 25 testes de banco; não foi tratada como validação completa. Fluxo público10/10, lista/envio71/71, detalhe37/37 e PWA36/36 passaram. As suítes se sobrepõem; não somar como testes únicos. Lint, TypeScript e build passaram (`tmp/check-v41-built.log`, `tmp/build-v41-final.log`).

A build foi reiniciada somente em127.0.0.1:3117, após confirmar zero AiRuns ativos e nenhum job em processamento. Após recarregar no navegador, o histórico preservou os quatro protocolos da sessão; “Ver resumo” exibiu a conclusão guardada, com aviso de que não é nova consulta, e “Enviar nova nota” retornou ao formulário mantendo o histórico. Nenhum novo PDF foi enviado nesta rodada. A skill de computer-use orientou a recarga após build e a confirmação do estado final.

A captura de imagem continuou indisponível. Não há nova homologação visual por screenshot ou em PWA físico. O fluxo está disponível para inspeção em http://localhost:3117/enviar-nota e http://localhost:3117/revisao/notas.

**Não integrar como Harness concluído:** a releitura por página ainda é diagnóstica, não integrada à extração normal; o teste completo do novo transporte não concluiu; os achados e a leitura incorreta do Roger anterior não foram corrigidos artificialmente. A próxima validação precisa demonstrar melhoria no fluxo completo, sem substituir resultados por um gabarito conhecido. A aplicação principal permanece intocada.
