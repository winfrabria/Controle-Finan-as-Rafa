# Rodada44 — conclusão bloqueada por metadados de cobertura

A NF1322 enviada pelo usuário (`cd968d76-fc6b-42fd-b5c1-33cd653de36e`) estava NO_PARAMETER/Análise incompleta apesar de descoberta e verificação terem concluído sem divergências. O validador rejeitava uma comparação monetária em `document:total`, e uma observação OTHER fora do índice da página mantinha a extração limitada. A observação também tinha uma data não presente no seu próprio trecho; a resposta independente já citava corretamente data e valor na mesma fonte/página.

Política final2026-09-12.10: uma comparação opcional de total é válida somente com dois índices distintos, evidências monetárias e estado coerente; conflito exige achado vinculado e valores rastreados. Comparações arbitrárias em checks de cobertura continuam inválidas. Lacunas de índice/proveniência podem ser resolvidas quando a verificação está completa e confirma os valores/datas nas fontes e registros correspondentes. A projeção usada para conferir o índice não é salva por cima da extração, não completa linhas ausentes e não altera valores. Cobertura desconhecida, página/linha faltante, fonte errada ou divergência sem prova continuam bloqueando aprovação.

Uma observação consultiva da descoberta não bloqueia a conclusão quando a própria descoberta declara evidência suficiente e o verificador confirma cobertura completa, sem limitações. Isso evita transformar ausência de documento externo não exigido em requisito automático. Necessidades de contexto e lacunas materiais continuam passando pelas regras existentes.

Na interface compartilhada, o bloco de alcance passou a ficar recolhido em **Detalhes da conferência**. Falhas explícitas de processamento e avisos de apontamento ainda não confirmado não foram convertidos em OK nem apagados.

## Validação

Replay offline da resposta original: cobertura completa e lacuna resolvível, sem escrita ou IA. Uma reauditoria real, sem nova extração, usou descoberta5,965s/US$0,011283 e verificação38,716s/US$0,0375765. Ambas concluíram. O teste revelou que a guarda recém-adicionada também rejeitava observação consultiva da descoberta; essa guarda foi corrigida. A resposta real já salva foi revalidada pelo pipeline em modo offline, com conferência de versão, hash do documento e hash da descoberta, **zero chamadas novas**. Resultado final: **OK/MEDIUM, nenhum achado, versão14**. Extração e original permaneceram idênticos. Custo conhecido total desta rodada: **US$0,0488595**.

Relatórios privados: `tmp/inspect-user-incomplete.json`, `tmp/reaudit-snapshot-0d0fa906-16d1-4fff-b103-5fecfa6f3841.json`, `tmp/reaudit-snapshot-d3970f51-537d-4c4a-adec-c6b8a376e5c6.json`. Não publicar. Demais notas do usuário não foram reprocessadas: Roger continua Suspeita; NF8957 e NF58 continuam OK. Há quatro notas, seis jobs concluídos e zero AiRuns ativos (`tmp/status-v44-final.json`).

**664/664 testes Harness com banco isolado**, zero pulados (`tmp/harness-v44-delivery.log`). Detalhe37/37, lista/envio72/72 e PWA36/36 passaram. Lint/TypeScript e build/postbuild passaram (`tmp/check-v44-final-all.log`, `tmp/build-v44-final.log`). A primeira suíte foi interrompida por Docker/banco indisponíveis; o Docker foi restabelecido sem recriar volumes, e a suíte completa foi repetida. Nenhuma nota do usuário foi apagada. A aplicação principal não foi alterada; permanece a worktree isolada na porta3117.
