# Prompts 2026-08-13.2

Os prompts executáveis ficam em `src/lib/audit-harness/prompts.ts`.

A extração agora exige `documentKind` e `evidenceObservations` por item em
documentos compostos. Cada valor visual deve manter seu papel: ficha, recibo,
venda, pagamento ou desconto. O modelo não pode escolher somente um valor
quando o anexo mostra registros concorrentes.

A auditoria continua percorrendo todas as despesas, sem parar no primeiro
achado. Divergências internas são achados; perguntas permanecem reservadas a
fatos externos ausentes que possam alterar a conclusão.
